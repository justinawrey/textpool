import React from 'react'
import styled from 'styled-components'

const StyledCode = styled.h1`
    align-self: center;
    color: black;
`

export default ({ code }) => <StyledCode>{code}</StyledCode>
